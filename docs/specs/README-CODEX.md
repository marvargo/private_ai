# Codex handoff

The repository specifications are intentionally split so each Codex task can load a small durable instruction set. Begin with `CODEX_START_PROMPT.md`, then follow `MASTER_EXECUTION_PLAN.md` and the first incomplete ledger item. Do not paste the entire production specification into a chat prompt.