import { ApiResourcePage } from '../../../components/ApiResourcePage';

export default function Page() {
  return <ApiResourcePage title="Knowledge" description="Knowledge documents and future ingestion sources for private context." endpoint="/state" actions={[]} />;
}
