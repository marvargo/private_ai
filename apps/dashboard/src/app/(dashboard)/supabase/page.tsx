import { ApiResourcePage } from '../../../components/ApiResourcePage';

export default function Page() {
  return <ApiResourcePage title="Supabase" description="Project connections, schema reads, migration drafts, and approval-gated apply flows." endpoint="/supabase/projects" actions={[]} />;
}
