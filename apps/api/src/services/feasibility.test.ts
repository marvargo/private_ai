import { describe, expect, it } from 'vitest'; import { checkLlama405BFeasibility } from './feasibility.js'; import { encryptSecret, decryptSecret } from './crypto.js'; import { assertStartAllowed } from './costControls.js';
describe('core safeguards',()=>{ it('fails safely on undersized hardware',()=>{ const r=checkLlama405BFeasibility({gpuCount:1,gpuModel:'RTX 4090',totalVramGb:24,diskGb:500,cudaAvailable:true,dockerAvailable:true,hfTokenPresent:false,modelPathAvailable:false,servingEngine:'vllm'}); expect(r.ok).toBe(false); expect(r.missing.join(' ')).toContain('8 GPUs'); }); it('encrypts and decrypts credentials',()=>{ const c=encryptSecret('secret','test-key'); expect(decryptSecret(c,'test-key')).toBe('secret'); }); it('blocks sessions over four hours',()=>{ expect(assertStartAllowed(100,5,{maxSessionHours:4}).allowed).toBe(false); }); });
